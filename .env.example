# Django Configuration
SECRET_KEY=your-secret-key-here
DEBUG=False
DJANGO_SETTINGS_MODULE=elite_wealth_capital.settings
ALLOWED_HOSTS=localhost,127.0.0.1,elitewealthcapita.uk,www.elitewealthcapita.uk

# Database (PostgreSQL on Render) - PERSISTENT & SECURE
# This database is SEPARATE from your app - data NEVER lost on redeployment
# Use INTERNAL URL for faster/secure connection:
# Format: postgresql://username:password@internal-hostname/database_name
DATABASE_URL=postgresql://elite_wealth_k85f_user:password@dpg-d72e1uh4tr6s73bdvfp0-a/elite_wealth_k85f

# Email Configuration (Zoho SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.zoho.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=admin@elitewealthcapita.uk
EMAIL_HOST_PASSWORD=your-zoho-app-password
DEFAULT_FROM_EMAIL=admin@elitewealthcapita.uk
ADMIN_EMAIL=admin@elitewealthcapita.uk

# Cloudinary Configuration (REQUIRED for production - stores images permanently)
# Sign up at https://cloudinary.com and get your credentials
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Redis (Optional - only if using Celery)
REDIS_URL=redis://localhost:6379/0

# Tawk.to Live Chat
TAWK_PROPERTY_ID=your-property-id
TAWK_WIDGET_ID=your-widget-id
TAWK_API_KEY=your-api-key

# Company Information
COMPANY_NAME=Elite Wealth Capital
COMPANY_EMAIL=admin@elitewealthcapital.uk
COMPANY_PHONE=+44-XXX-XXXX-XXX
COMPANY_ADDRESS=London, United Kingdom
COMPANY_WEBSITE=https://elitewealthcapita.uk

# CSRF Trusted Origins (comma-separated)
CSRF_TRUSTED_ORIGINS=https://elitewealthcapita.uk,https://www.elitewealthcapita.uk

# ============================================================
# DATA PERSISTENCE NOTES
# ============================================================
# ✅ All user data stored in PostgreSQL (persistent, never lost)
# ✅ Profile images stored in Cloudinary (persistent, CDN-backed)
# ✅ Database survives all redeployments
# ✅ Render provides automatic daily backups
# ✅ Use internal DATABASE_URL for better performance
# 
# To backup manually: python backup_database.py
# To restore: python manage.py loaddata backups/backup_YYYYMMDD.json
# ============================================================

